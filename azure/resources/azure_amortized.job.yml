resources:
  jobs:
    azure_amortized_job:
      name: azure_amortized_job

      trigger:
        file_arrival:
          url: ${var.volume_path}azure-amortized-cost/
        pause_status: PAUSED

      tasks:
        - task_key: refresh_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.azure_amortized_pipeline.id}
